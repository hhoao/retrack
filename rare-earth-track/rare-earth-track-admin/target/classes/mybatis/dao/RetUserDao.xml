<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rare_earth_track.mgb.dao.RetUserDao">
    <resultMap id="retUser" type="com.rare_earth_track.mgb.model.RetUser">
        <id column="uid" property="id"/>
        <result column="name" property="name"/>
        <result column="sex" property="sex"/>
        <result column="age" property="age"/>
        <result column="birthday" property="birthday"/>
        <result column="address" property="address"/>
        <result column="password" property="password"/>

    </resultMap>
    <select id="getUserByName" resultMap="retUser">
        SELECT *  FROM ret_user WHERE name=#{name};
    </select>
    <select id="getAllUsers" resultMap="retUser">
        SELECT * FROM ret_user;
    </select>
    <insert id="addUser" parameterType="com.rare_earth_track.mgb.model.RetUser">
        <selectKey resultType="java.lang.Integer" keyProperty="uid" keyColumn="uid" order="AFTER">
            SELECT LAST_INSERT_ID();
        </selectKey>
        INSERT INTO ret_user(name, sex, age, birthday, address, password) VALUES(#{retUser.name}, #{retUser.sex}, #{retUser.age}, #{retUser.birthday}, #{retUser.address}, #{retUser.password});
    </insert>
</mapper>